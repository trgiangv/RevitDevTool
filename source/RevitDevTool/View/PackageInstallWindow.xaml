<Window x:Class="RevitDevTool.View.PackageInstallWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:design="clr-namespace:RevitDevTool.Theme.Design"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModel="clr-namespace:RevitDevTool.ViewModel"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        Title="Install Dependencies"
        Width="400"
        Height="120"
        d:DesignHeight="120"
        d:DesignWidth="400"
        Background="{DynamicResource MahApps.Brushes.ThemeBackground}"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        d:DataContext="{d:DesignInstance Type=viewModel:PackageInstallViewModel}"
        WindowStartupLocation="CenterOwner"
        mc:Ignorable="d">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/RevitDevTool;component/Theme/Theme.xaml" />
                <design:IntellisenseResources Source="/RevitDevTool;component/Theme/Design/DesignTimeResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="20,15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="15" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   FontSize="10"
                   Foreground="{DynamicResource MahApps.Brushes.ThemeForeground}"
                   Text="{Binding StatusMessage}"
                   TextAlignment="Center" />

        <mah:MetroProgressBar Grid.Row="2"
                              Height="15"
                              IsIndeterminate="True" />
    </Grid>
</Window>
