# Documentation assets

Static assets are grouped by purpose:

- `icons/` — logos and UI icons used by the documentation site.
- `images/` — screenshots, diagrams, and illustrations embedded in pages.
- `downloads/` — files intended for direct download.

Reference assets from Markdown with root-relative site paths, for example
`/RevitDevTool/images/example.png`.
